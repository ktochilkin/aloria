# Оптимизации для iOS Safari Web

Добавлены следующие улучшения для плавности веб-версии на iPhone:

## ✅ Внесенные изменения

### 1. **HTML оптимизации** ([web/index.html](web/index.html))
- ✅ Добавлен `minimal-ui` в viewport для полноэкранного режима
- ✅ GPU ускорение через CSS transforms
- ✅ `-webkit-overflow-scrolling: touch` для плавной прокрутки
- ✅ Отключение подсветки тапов и выделения текста
- ✅ Дополнительные iOS мета-теги

### 2. **Кастомный ScrollBehavior** ([lib/core/platform/web_scroll_behavior.dart](lib/core/platform/web_scroll_behavior.dart))
- ✅ `BouncingScrollPhysics` для естественной прокрутки как на iOS
- ✅ Поддержка всех типов ввода (touch, mouse, stylus, trackpad)

### 3. **Оптимизация сборки** ([build.sh](build.sh), [build_ios_web.sh](build_ios_web.sh))
- ✅ **CanvasKit рендерер** вместо HTML (лучше для iOS)
- ✅ Release mode с оптимизациями
- ✅ Создан отдельный скрипт для iOS веб-сборки

## 🚀 Как пересобрать

```bash
# Используйте оптимизированный скрипт
./build_ios_web.sh

# Или вручную
flutter build web --release --web-renderer canvaskit
```

## 🧪 Тестирование

1. Пересоберите проект
2. Откройте на реальном iPhone в Safari
3. Проверьте плавность прокрутки и анимаций

## 📊 Ожидаемые улучшения

- **Плавная прокрутка**: как в нативном приложении
- **Быстрый рендеринг**: CanvasKit использует GPU
- **Нет задержек тапов**: отключена подсветка
- **Естественная физика**: BouncingScrollPhysics

## 🔍 Дополнительная отладка

Если все еще есть проблемы:
1. Откройте Safari Web Inspector (на Mac)
2. Подключите iPhone через USB
3. Проверьте вкладку Timeline для узких мест
4. Посмотрите Console на ошибки

## 📚 Документация

См. [WEB_OPTIMIZATION.md](WEB_OPTIMIZATION.md) для деталей.
